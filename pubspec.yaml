name: transport_manager
description: Transport Fleet Management App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: 2.3.0
  path: ^1.9.1
  pdf: ^3.10.4
  printing: ^5.11.0
  share_plus: ^7.2.1
  url_launcher: ^6.2.1
  path_provider: ^2.1.1

dependency_overrides:
  sqflite_android: 2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
