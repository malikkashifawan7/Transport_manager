name: transport_hisab
description: Offline-first transport manager
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  pdf: ^3.10.8
  printing: ^5.13.0

flutter:
  uses-material-design: true
  
